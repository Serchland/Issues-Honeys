<UserControl x:Class="IssuesHoneys.Modules.Projects.Views.AppMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             BorderBrush="{StaticResource MainBorderBrush}"
             BorderThickness="1">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Content="Navigate"
                Command="{Binding NavigateCommand}"
                CommandParameter="DemoNavigate"></Button>
        <Label Grid.Row="1" Content="{Binding Message}" FontSize="50"></Label>
    </Grid>
</UserControl>
